"use client";

import React, { useState } from "react";
import { ServicePageShell } from "@/components/ServicePageShell";
import { Camera, User, Sparkles, Wand2 } from "lucide-react";
import { GlassContainer } from "@/components/GlassContainer";
import { translations } from "@/lib/i18n";

export default function AvatarBuilderPage() {
  const [lang] = useState<"ge" | "en">("ge");
  const t = translations[lang];

  const features = [
    { icon: Camera, title: lang === "ge" ? "ფოტოდან ავატარი" : "Photo to Avatar", desc: lang === "ge" ? "ატვირთე სურათი და შექმენი 3D სტილის ავატარი" : "Upload photo and create 3D style avatar" },
    { icon: User, title: lang === "ge" ? "ტექსტური აღწერა" : "Text Description", desc: lang === "ge" ? "აღწერე როგორი ავატარი გინდა და AI შექმნის" : "Describe your avatar and AI will generate it" },
    { icon: Sparkles, title: lang === "ge" ? "სტილის ვარიაციები" : "Style Variations", desc: lang === "ge" ? "ანიმე, რეალისტური, 3D, პიქსელ არტი" : "Anime, realistic, 3D, pixel art" },
    { icon: Wand2, title: lang === "ge" ? "რედაქტირება" : "Edit", desc: lang === "ge" ? "შეცვალე ემოციები, პოზა და გარემო" : "Change emotions, pose, and environment" },
  ];

  return (
    <ServicePageShell
      title={lang === "ge" ? "ავატარის შექმნა" : "Avatar Builder"}
      subtitle={lang === "ge" ? "AI ავატარის გენერატორი" : "AI Avatar Generator"}
      description={lang === "ge" ? "შექმენი პერსონალური AI ავატარი სურათის ან ტექსტის მიხედვით" : "Create personal AI avatar from photo or text description"}
      icon="/icons/services/avatar-builder.webp"
      lang={lang}
    >
      <div className="grid grid-cols-1 md:grid-cols-2 gap-4">
        {features.map((feature, idx) => (
          <GlassContainer key={idx} className="p-6" glow={false}>
            <div className="flex items-start gap-4">
              <div className="p-3 rounded-xl bg-gradient-to-br from-cyan-500/20 to-blue-600/20 border border-cyan-500/30">
                <feature.icon className="w-6 h-6 text-cyan-400" />
              </div>
              <div>
                <h3 className="font-medium mb-1 text-white">{feature.title}</h3>
                <p className="text-sm text-slate-400">{feature.desc}</p>
              </div>
            </div>
          </GlassContainer>
        ))}
      </div>
    </ServicePageShell>
  );
}
