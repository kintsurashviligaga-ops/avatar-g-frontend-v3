'use client';

import { MusicStudio } from '@/components/studio/MusicStudio';

// Modern, dedicated music generator: in-app voice recorder (sing in YOUR voice),
// cover/remix, custom polished player. Replaces the old generic AgentShell page.
export default function MusicStudioPage() {
  return <MusicStudio />;
}
