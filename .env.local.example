# =======================
# CORE SERVICES (REQUIRED)
# =======================

# Supabase - Database & Authentication
# Get from: https://supabase.com/ → Project Settings → API
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# =======================
# AVATAR GENERATION (REQUIRED FOR PRODUCTION)
# =======================

# Stability AI - REQUIRED for avatar generation endpoint
# Get from: https://platform.stability.ai/ → Account → API Keys
# Without this key, avatar generation will fail with 500 error
STABILITY_API_KEY=sk-your-stability-key
STABILITY_ENGINE=stable-diffusion-xl-1024-v1-0

# =======================
# PROVIDER MODE (SERVER-SIDE ONLY)
# =======================

# Controls whether to use real API providers or mock implementations
# 'mock' = Free, no API calls, instant results (for testing/development)
# 'real' = Actual provider APIs, requires API keys, real generations
# Default: mock (safe for development)
# NOTE: This is SERVER-SIDE ONLY. Client cannot override.
PROVIDER_MODE=mock

# =======================
# CLOUDFLARE R2 STORAGE (OPTIONAL - Falls back to Supabase if missing)
# =======================

# R2 Object Storage - For storing avatars and generated content
# Get from: https://dash.cloudflare.com/ → R2 → API Tokens
# ✅ Configured in production (Updated Feb 2-3, 2026)
R2_ACCOUNT_ID=your-account-id
R2_ACCESS_KEY_ID=your-access-key-id
R2_SECRET_ACCESS_KEY=your-secret-access-key
R2_BUCKET_NAME=your-bucket-name
R2_ENDPOINT=https://your-account-id.r2.cloudflarestorage.com
R2_REGION=auto

# =======================
# API ENDPOINTS (REQUIRED)
# =======================

# Main API endpoint for frontend
# ✅ Configured in production (Added Jan 16)
NEXT_PUBLIC_API_URL=https://your-api-domain.com

# Pentagon API endpoint
# ✅ Configured in production (Updated Jan 23)
NEXT_PUBLIC_PENTAGON_API_URL=https://your-pentagon-api.com

# App Configuration
NEXT_PUBLIC_APP_URL=https://avatar-g-frontend-v3.vercel.app

# Frontend origin (required for OpenRouter CORS)
NEXT_PUBLIC_FRONTEND_ORIGIN=https://avatar-g-frontend-v3.vercel.app

# =======================
# AI SERVICES (OPTIONAL - Has mock fallback)
# =======================

# Runway ML - Video Generation (optional, has mock fallback)
# Get from: https://runwayml.com/ → Settings → API Access
RUNWAY_API_KEY=key-your-runway-key
RUNWAY_MODEL=gen2

# Replicate - Music & Video Alternative (optional, has mock fallback)
# Get from: https://replicate.com/ → Account Settings
REPLICATE_API_TOKEN=r8_your-replicate-token

# ElevenLabs - Voice/Audio Generation (optional, has mock fallback)
# Get from: https://elevenlabs.io/ → Profile → API Keys
ELEVENLABS_API_KEY=your-elevenlabs-key
ELEVENLABS_VOICE_ID=your-default-voice-id

# =======================
# AI SERVICES (OPTIONAL)
# =======================

# =======================
# LLM SERVICES (OPTIONAL - Fallback chain)
# =======================

# OpenAI - Primary chat/text fallback
# Get from: https://platform.openai.com/ → API Keys
OPENAI_API_KEY=sk-your-openai-key-here
OPENAI_ORG_ID=org-your-org-id

# OpenRouter - Alternative LLM routing
# Get from: https://openrouter.ai/ → Settings → API Keys
OPENROUTER_API_KEY=sk-or-your-openrouter-key

# Groq - Fast inference alternative
# Get from: https://console.groq.com/ → API Keys
GROQ_API_KEY=your-groq-key

# DeepSeek - Alternative LLM provider
# Get from: https://platform.deepseek.com/ → API Keys
DEEPSEEK_API_KEY=your-deepseek-key

# xAI (Grok) - Alternative LLM provider
# Get from: https://console.x.ai/ → API Keys
XAI_API_KEY=your-xai-key

# Google Cloud - Alternative TTS provider (if not using ElevenLabs)
GOOGLE_TTS_API_KEY=your-google-tts-key

# =======================
# SECURITY (OPTIONAL)
# =======================

# JWT and Encryption Keys (for custom auth features)
JWT_SECRET=your-jwt-secret-min-32-characters
ENCRYPTION_KEY=your-encryption-key-32-chars

# =======================
# VARIABLE SCOPING GUIDE
# =======================

# SERVER-ONLY (Never expose to browser, no NEXT_PUBLIC_ prefix):
#   - SUPABASE_SERVICE_ROLE_KEY (database bypass)
#   - STABILITY_API_KEY (avatar generation)
#   - REPLICATE_API_TOKEN (music/video generation)
#   - ELEVENLABS_API_KEY (voice generation)
#   - RUNWAY_API_KEY (video generation)
#   - All LLM API keys (OpenAI, OpenRouter, Groq, DeepSeek, xAI, Google)
#   - PROVIDER_MODE (provider selection)
#   - JWT_SECRET, ENCRYPTION_KEY
#   - R2_* credentials
#   - STABILITY_ENGINE, RUNWAY_MODEL

# PUBLIC (Safe to expose via NEXT_PUBLIC_ prefix):
#   - NEXT_PUBLIC_SUPABASE_URL (endpoint only, no auth)
#   - NEXT_PUBLIC_SUPABASE_ANON_KEY (limited by RLS)
#   - NEXT_PUBLIC_API_URL
#   - NEXT_PUBLIC_PENTAGON_API_URL
#   - NEXT_PUBLIC_FRONTEND_ORIGIN
#   - NEXT_PUBLIC_APP_URL

# =======================
# SETUP INSTRUCTIONS
# =======================

# For LOCAL development:
# 1. Copy this file to .env.local
# 2. Fill in the REQUIRED values below
# 3. Optional values can be left blank (will use mock/fallback)
# 4. Run: npm run dev

# For PRODUCTION on Vercel:
# 1. Go to Vercel Project → Settings → Environment Variables
# 2. Add all REQUIRED variables (see list below)
# 3. Optionally add other provider keys for full features
# 4. Redeploy

# REQUIRED FOR PRODUCTION (blocking):
#   - NEXT_PUBLIC_SUPABASE_URL
#   - NEXT_PUBLIC_SUPABASE_ANON_KEY
#   - SUPABASE_SERVICE_ROLE_KEY
#   - STABILITY_API_KEY (for avatar generation)

# OPTIONAL FOR PRODUCTION (has fallback):
#   - All other variables listed above
#   - System will use mock implementations if keys are missing
