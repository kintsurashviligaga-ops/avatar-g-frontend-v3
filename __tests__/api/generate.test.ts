/**
 * Legacy generate routes were removed in favor of service-specific APIs.
 */

describe.skip('Legacy /api/generate routes', () => {
  test('placeholder', () => {
    expect(true).toBe(true);
  });
});
